# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: sc2-msspe-bioinfo
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bioconda::fastqc=0.11.9
  - bioconda::multiqc=1.8
  - samtools=1.9
  - trim-galore=0.6.5
  - minimap2=2.17
  - ivar=1.1_beta
